<section class="logo-section-wrapper">
  <div class="logo-section-content">
    <div class="logo_position">
      <div class="logo-list border--{{ section.id }}">
          <div class="header">
            <div class="logo-title">{{ section.settings.heading | escape }}</div>
            <div class="container-arrows">
              <div class="prev-review-slide3">{% include 'review_arrow_left' %}</div>
              <div class="next-review-slide3">{% include 'review_arrow_right' %}</div>
            </div>
          </div>
        <div id="logo-wrapper">
        {% for block in section.blocks %}
          {% if block.settings.image != blank %}
            <div class="logo">
              <a href="{{ block.settings.link }}" target="_blank">
                <img src="{{ block.settings.image | img_url: 'master' }}" alt="logo">
              </a>
            </div>
          {% endif %}
        {% endfor %}
        </div>
      </div>
    </div>
  </div>
</section>

{{ 'slick.min.js' | asset_url | script_tag }}

<script>

      $('#logo-wrapper').slick({
        dots: false,
        infinite: true,
        speed: 300,
        centerMode: true,
        slidesToShow: 4,
        slidesToScroll: 1,
        prevArrow: $('.prev-review-slide3'),
        nextArrow: $('.next-review-slide3'),
          responsive: [
                  {
                    breakpoint: 1300,
                    settings: {
                      slidesToShow: 3,
                      slidesToScroll: 1
                    }
                  },
                  {
                      breakpoint: 768,
                      settings: {
                        slidesToShow: 3,
                        slidesToScroll: 1
                      }
                  },
                  {
                      breakpoint: 500,
                      settings: {
                          slidesToShow: 2,
                          slidesToScroll: 1,
                      }
          }
          ]       
      })
</script>

<style>
  .review_arrow_left{
    margin-right: 20px;
  }
  .slick-list{
    padding: 0!important;
  }
  .logo_position{
  max-width: 100%;
  }
  .prev-slide1{
    margin-right: 20px;
  }
  .container-arrows{
    display: flex;
  }
  .header{
    width: 100%;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding-right: 165px;
    margin-bottom: 2px;
  }
  .logo-section-content{
    max-width: 100%;
    margin: 80px 0 109px 165px;
  }
  .logo-title{
    font-style: normal;
    font-weight: 400;
    font-size: 32px;
    line-height: 42px;
    text-align: center;
    letter-spacing: 1.6px;
    color: #222222;
    font-family: "Graphik-Regular";
  }
  .logo-list{
    justify-content: center;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
  }
  #logo-wrapper{
    display: flex;
    justify-content: center;
    align-items: center;
    width: 100%;
  }
  .logo{
    display: flex !important;
    margin: 0 60px 0 0;
    min-height: 80px;
  }
  .logo img{
    height: 100%;
    object-fit: contain;
    max-width: 300px!important;
    filter: brightness(100%);
    transition-duration: 500ms;
  }
  .logo img:hover{
    filter: brightness(20%);
    transition-duration: 500ms;
  }
  .logo_position .slick-list{
    padding: 0 85px 0 0;
  }
  .border--{{ section.id }}{
  {% if section.settings.show-border %}
    border-bottom: 1px solid #E9ECEE;
  {% endif %}
  }
  .logo-section-wrapper{
    background-color: {{ section.settings.bg-color }};
    margin-bottom: -12px;
    width: 100%;
  }
  @media (max-width: 1250px){
    .header{
      padding-right: 80px;
    }
    .logo-section-content{
      margin-left: 80px;
    }
    .logo img{
      width: 100%;
    }
  }

  @media (max-width: 996px) {
    .logo-title{
      font-size: 24px;
      line-height: 42px;
    }
    .header{
      padding-bottom: 0;
    }
    .logo-section-content{
      margin-right: 0px;
      margin-left: 0px;
    }
    #logo-wrapper {
      justify-content: center;
      margin: 0 auto;
    }
    .logo-title{
      margin-right: 0;
      text-align: left;
      width: 100%;
    }
  }

  @media (max-width: 970px){
    .header{
      padding-right: 30px;
    }
    .logo-list{
      padding-left: 30px;
    }
  }

  @media (max-width: 768px){
    .header{
      padding-right: 16px;
    }
    .logo-list {
      padding-left: 16px;
    }
    .logo-section-content{
      margin: 50px 0;
    }

  @media (max-width: 480px) {
    .prev-review-slide3 svg, .next-review-slide3 svg {
      width: 44px;
      height: 44px;
    }
    .review_arrow_left{
      margin-right: 10px;
    }
    .logo-list {
      padding-bottom: 0;
      justify-content: center;
    }
    #logo-wrapper .logo{
      padding: 0;
      width: 50%;
      text-align: center;
      margin: 0;
    }
    .logo a{
      max-width: 75%;
      margin: 0 auto;
      display: block;
    }
  }
  @media (min-width: 1540px) {
    #logo-wrapper{
      min-width: auto;
      justify-content: space-between;
    }

  }
</style>

{% schema %}
{
  "name": "Mindflow logo list",
  "class": "logo-section",
  "max_blocks": 20,
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
	  "default": "Default heading"
    },
{
"type": "color",
"id": "bg-color",
"label": "Background color"
},
{
"type": "checkbox",
"id": "show-border",
"label": "Bottom border"
}
  ],
  "blocks": [
    {
      "type": "logo_image",
      "name": "Logo",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Mindflow logo list",
      "category": "Mindflow",
      "blocks": [
        {
          "type": "logo_image"
        },
        {
          "type": "logo_image"
        },
        {
          "type": "logo_image"
        }
      ]
    }
  ]
}
{% endschema %}
